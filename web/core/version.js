// The contracts version the WEB app was last synced to. Bump together with
// contracts/manifest.json (and the iOS App/Core/ContractsVersion.swift) whenever
// shared behavior — the cycle algorithm, models, or tokens — changes.
export const CONTRACTS_VERSION = "1.1.0";
